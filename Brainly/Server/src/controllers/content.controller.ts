export const createContent = async (req: any, res: any) => {
  res.send("This is Create Content Endpoint");
};

export const displayContent = async (req: any, res: any) => {
  res.send("This is Create Content Endpoint");
};

export const deleteContent = async (req: any, res: any) => {
  res.send("This is Create Content Endpoint");
};
