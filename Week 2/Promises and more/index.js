/*const rect1 = {
  width: 2,
  height: 4,
  color: "red",
};

function area(rect) {
  return rect.width * rect.height;
}

const ans = area(rect1);
console.log(ans);

console.log(rect1);
*/

/*
class Rectangle {
  constructor(width, hheight, color) {
    this.width = width;
    this.hheight = hheight;
    this.color = color;
  }

  area() {
    const area = this.width * this.hheight;
    return area;
  }

  paint() {
    console.log(`painting with colour ${this.color}`);
  }
}

const rect = new Rectangle(2, 4, "red");
const area = rect.area();
console.log(area);

rect.paint();
*/

//  IN built calsses in javascript

/*
// Date() class
const date = new Date();
console.log(date.getFullYear());
*/

/*
// Map Class
const map = new Map();
map.set("name", "Kunal");
map.set("age", 19);

const firstname = map.get("name");
const age = map.get("age");

console.log(firstname, age);
*/
